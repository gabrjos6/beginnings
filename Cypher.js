
function cypher() {
  var a = prompt("a"); 
  if(a === "å" || a === "Å") {
    a = "a" 
    }
    if(a === "∫" || a === "ı") {
    a = "b"
    }
    if(a === "ç" || a === "Ç") {
    a = "c"
    }
    if(a === "∂" || a === "Î") {
    a = "d"
    }
    if(a === "´" || a === "´") {
    a = "e"
    }
    if(a === "ƒ" || a === "Ï") {
    a = "f"
    }
    if(a === "©" || a === "˝") {
    a = "g"
    }
    if(a === "˙" || a === "Ó") {
    a = "h"
    }
    if(a === "ˆ" || a === "ˆ") {
    a = "i"
    }
    if(a === "∆" || a === "Ô") {
    a = "j"
    }
    if(a === "˚" || a === "") {
    a = "k"
    }
    if(a === "¬" || a === "Ò") {
    a = "l"
    }
    if(a === "µ" || a === "Â") {
    a = "m"
    }
    if(a === "˜" || a === "˜") {
    a = "n"
    }
    if(a === "ø" || a === "Ø") {
    a = "o"
    }
    if(a === "π" || a === "∏") {
    a = "p"
    }
    if(a === "œ" || a === "Œ") {
    a = "q"
    }
    if(a === "®" || a === "‰") {
    a = "r"
    }
    if(a === "ß" || a === "Í") {
    a = "s"
    }
    if(a === "†" || a === "ˇ") {
    a = "t"
    }
    if(a === "¨" || a === "¨") {
    a = "u"
    }
    if(a === "√" || a === "◊") {
    a = "v"
    }
    if(a === "∑" || a === "„") {
    a = "w"
    }
    if(a === "≈" || a === "˛") {
    a = "x"
    }
    if(a === "¥" || a === "Á") {
    a = "y"
    }
    if(a === "Ω" || a === "¸") {
    a = "z"
    }
var b = prompt("b"); 
  if(b === "å" || b === "Å") 
    b = "a" 
    }
    if(b === "∫" || b === "ı") {
    b = "b"
    }
    if(b === "ç" || b === "Ç") {
    b = "c"
    }
    if(b === "∂" || b === "Î") {
    b = "d"
    }
    if(b === "´" || b === "´") {
    b = "e"
    }
    if(b === "ƒ" || b === "Ï") {
    b = "f"
    }
    if(b === "©" || b === "˝") {
    b = "g"
    }
    if(b === "˙" || b === "Ó") {
    b = "h"
    }
    if(b === "ˆ" || b === "ˆ") {
    b = "i"
    }
    if(b === "∆" || b === "Ô") {
    b = "j"
    }
    if(b === "˚" || b === "") {
    b = "k"
    }
    if(b === "¬" || b === "Ò") {
    b = "l"
    }
    if(b === "µ" || b === "Â") {
    b = "m"
    }
    if(b === "˜" || b === "˜") {
    b = "n"
    }
    if(b === "ø" || b === "Ø") {
    b = "o"
    }
    if(b === "π" || b === "∏") {
    b = "p"
    }
    if(b === "œ" || b === "Œ") {
    b = "q"
    }
    if(b === "®" || b === "‰") {
    b = "r"
    }
    if(b === "ß" || b === "Í") {
    b = "s"
    }
    if(b === "†" || b === "ˇ") {
    b = "t"
    }
    if(b === "¨" || b === "¨") {
    b = "u"
    }
    if(b === "√" || b === "◊") {
    b = "v"
    }
    if(b === "∑" || b === "„") {
    b = "w"
    }
    if(b === "≈" || b === "˛") {
    b = "x"
    }
    if(b === "¥" || b === "Á") {
    b = "y"
    }
    if(b === "Ω" || b === "¸") {
    b = "z"
    }
  var c = prompt("c"); 
if(c === "å" || c === "Å") {
    c = "a" 
    }
    if(c === "∫" || a === "ı") {
    c = "b"
    }
    if(c === "ç" || a === "Ç") {
    c = "c"
    }
    if(c === "∂" || a === "Î") {
    c = "d"
    }
    if(c === "´" || a === "´") {
    c = "e"
    }
    if(c === "ƒ" || a === "Ï") {
    c = "f"
    }
    if(c === "©" || a === "˝") {
    c = "g"
    }
    if(c === "˙" || a === "Ó") {
    c = "h"
    }
    if(c === "ˆ" || a === "ˆ") {
    c = "i"
    }
    if(c === "∆" || a === "Ô") {
    c = "j"
    }
    if(c === "˚" || a === "") {
    c = "k"
    }
    if(c === "¬" || a === "Ò") {
    c = "l"
    }
    if(c === "µ" || a === "Â") {
    c = "m"
    }
    if(c === "˜" || a === "˜") {
    c = "n"
    }
    if(c === "ø" || a === "Ø") {
    c = "o"
    }
    if(c === "π" || a === "∏") {
    c = "p"
    }
    if(c === "œ" || a === "Œ") {
    c = "q"
    }
    if(c === "®" || a === "‰") {
    c = "r"
    }
    if(c === "ß" || a === "Í") {
    c = "s"
    }
    if(c === "†" || a === "ˇ") {
    c = "t"
    }
    if(c === "¨" || a === "¨") {
    c = "u"
    }
    if(c === "√" || a === "◊") {
    c = "v"
    }
    if(c === "∑" || a === "„") {
    c = "w"
    }
    if(c === "≈" || a === "˛") {
    c = "x"
    }
    if(c === "¥" || a === "Á") {
    c = "y"
    }
    if(c === "Ω" || a === "¸") {
    c = "z"
    }
  alert(a + b + c);
}
