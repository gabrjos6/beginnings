
function cypher() {
  var a = prompt("a");
  var b = prompt("b");
  var c = prompt("c");
  if(a === "å" || a === "Å") {
    a = "a" 
    }
    if(a === "∫" || a === "ı") {
    a = "b"
    }
    if(a === "ç" || a === "Ç") {
    a = "c"
    }
    if(a === "∂" || a === "Î") {
    a = "d"
    }
    if(a === "´" || a === "´") {
    a = "e"
    }
    if(a === "ƒ" || a === "Ï") {
    a = "f"
    }
    if(a === "©" || a === "˝") {
    a = "g"
    }
    if(a === "˙" || a === "Ó") {
    a = "h"
    }
    if(a === "ˆ" || a === "ˆ") {
    a = "i"
    }
    if(a === "∆" || a === "Ô") {
    a = "j"
    }
    if(a === "˚" || a === "") {
    a = "k"
    }
    if(a === "¬" || a === "Ò") {
    a = "l"
    }
    if(a === "µ" || a === "Â") {
    a = "m"
    }
    if(a === "˜" || a === "˜") {
    a = "n"
    }
    if(a === "ø" || a === "Ø") {
    a = "o"
    }
    if(a === "π" || a === "∏") {
    a = "p"
    }
    if(a === "œ" || a === "Œ") {
    a = "q"
    }
    if(a === "®" || a === "‰") {
    a = "r"
    }
    if(a === "ß" || a === "Í") {
    a = "s"
    }
    if(a === "†" || a === "ˇ") {
    a = "t"
    }
    if(a === "¨" || a === "¨") {
    a = "u"
    }
    if(a === "√" || a === "◊") {
    a = "v"
    }
    if(a === "∑" || a === "„") {
    a = "w"
    }
    if(a === "≈" || a === "˛") {
    a = "x"
    }
    if(a === "¥" || a === "Á") {
    a = "y"
    }
    if(a === "Ω" || a === "¸") {
    a = "z"
    }
    if(b === "å" || b === "Å") 
    b = "a" 
    }
    if(b === "∫" || b === "ı") { 
    b = "b"
    }
    if(b === "ç" || b === "Ç") {
    b = "c"
    }
    if(b === "∂" || b === "Î") {
    b = "d"
    }
    if(b === "´" || b === "´") {
    b = "e"
    }
    if(b === "ƒ" || b === "Ï") {
    b = "f"
    }
    if(b === "©" || b === "˝") {
    b = "g"
    }
    if(b === "˙" || b === "Ó") {
    b = "h"
    }
    if(b === "ˆ" || b === "ˆ") {
    b = "i"
    }
    if(b === "∆" || b === "Ô") {
    b = "j"
    }
    if(b === "˚" || b === "") {
    b = "k"
    }
    if(b === "¬" || b === "Ò") {
    b = "l"
    }
    if(b === "µ" || b === "Â") {
    b = "m"
    }
    if(b === "˜" || b === "˜") {
    b = "n"
    }
    if(b === "ø" || b === "Ø") {
    b = "o"
    }
    if(b === "π" || b === "∏") {
    b = "p"
    }
    if(b === "œ" || b === "Œ") {
    b = "q"
    }
    if(b === "®" || b === "‰") {
    b = "r"
    }
    if(b === "ß" || b === "Í") {
    b = "s"
    }
    if(b === "†" || b === "ˇ") {
    b = "t"
    }
    if(b === "¨" || b === "¨") {
    b = "u"
    }
    if(b === "√" || b === "◊") {
    b = "v"
    }
    if(b === "∑" || b === "„") {
    b = "w"
    }
    if(b === "≈" || b === "˛") {
    b = "x"
    }
    if(b === "¥" || b === "Á") {
    b = "y"
    }
    if(b === "Ω" || b === "¸") {
    b = "z"
    } 
    if(c === "å" || c === "Å") {
    c = "a" 
    }
    if(c === "∫" || c === "ı") {
    c = "b"
    }
    if(c === "ç" || c === "Ç") {
    c = "c"
    }
    if(c === "∂" || c === "Î") {
    c = "d"
    }
    if(c === "´" || c === "´") {
    c = "e"
    }
    if(c === "ƒ" || c === "Ï") {
    c = "f"
    }
    if(c === "©" || c === "˝") {
    c = "g"
    }
    if(c === "˙" || c === "Ó") {
    c = "h"
    }
    if(c === "ˆ" || c === "ˆ") {
    c = "i"
    }
    if(c === "∆" || c === "Ô") {
    c = "j"
    }
    if(c === "˚" || c === "") {
    c = "k"
    }
    if(c === "¬" || c === "Ò") {
    c = "l"
    }
    if(c === "µ" || c === "Â") {
    c = "m"
    }
    if(c === "˜" || c === "˜") {
    c = "n"
    }
    if(c === "ø" || c === "Ø") {
    c = "o"
    }
    if(c === "π" || c === "∏") {
    c = "p"
    }
    if(c === "œ" || c === "Œ") {
    c = "q"
    }
    if(c === "®" || c === "‰") {
    c = "r"
    }
    if(c === "ß" || c === "Í") {
    c = "s"
    }
    if(c === "†" || c === "ˇ") {
    c = "t"
    }
    if(c === "¨" || c === "¨") {
    c = "u"
    }
    if(c === "√" || c === "◊") {
    c = "v"
    }
    if(c === "∑" || c === "„") {
    c = "w"
    }
    if(c === "≈" || c === "˛") {
    c = "x"
    }
    if(c === "¥" || c === "Á") {
    c = "y"
    }
    if(c === "Ω" || c === "¸") {
    c = "z"
    }
  alert(a + b + c);
